<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @land.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @land.description %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag(@land.image_url.to_s) %>
</p>

<%= link_to 'Edit', edit_land_path(@land) %> |
<%= link_to 'Back', lands_path %>
